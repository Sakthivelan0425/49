<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>49 ALL OUT</title>

<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow&display=swap" rel="stylesheet">

<style>
body{
  margin:0;
  background:#0B0B0B;
  color:white;
  font-family:'Barlow',sans-serif;
  display:flex;
  align-items:center;
  justify-content:center;
  height:100vh;
  text-align:center;
  overflow:hidden;
}

.bg{
  position:absolute;
  width:100%;
  height:100%;
  background:
    radial-gradient(circle at center, rgba(218,24,24,0.2), transparent 60%);
}

.container{
  position:relative;
  z-index:1;
}

/* RED TEXT */
.score{
  font-family:'Bebas Neue',sans-serif;
  font-size:180px;
  color:#DA1818;
  text-shadow:0 0 60px rgba(218,24,24,0.9);
  animation:pulse 2s infinite;
}

/* GOLD TEXT */
.tagline{
  font-family:'Bebas Neue',sans-serif;
  font-size:50px;
  color:#FFD700;
  letter-spacing:6px;
  text-shadow:0 0 25px rgba(255,215,0,0.6);
}

@keyframes pulse{
  0%,100%{text-shadow:0 0 40px rgba(218,24,24,0.8);}
  50%{text-shadow:0 0 100px rgba(218,24,24,1);}
}

.desc{
  margin-top:20px;
  color:rgba(255,255,255,0.6);
  font-size:14px;
}

.live{
  margin-top:30px;
  padding:12px 25px;
  border:1px solid rgba(218,24,24,0.3);
  display:inline-block;
  font-size:13px;
  letter-spacing:2px;
}

.live span{
  color:#FFD700;
  font-weight:bold;
}

/* BUTTON */
.btn{
  margin-top:30px;
  padding:14px 30px;
  background:#DA1818;
  border:none;
  color:white;
  font-weight:bold;
  letter-spacing:2px;
  cursor:pointer;
  transition:0.3s;
}

.btn:hover{
  background:#ff2a2a;
  box-shadow:0 0 20px rgba(218,24,24,0.6);
}

/* POPUP */
.popup{
  position:fixed;
  top:50%;
  left:50%;
  transform:translate(-50%, -50%) scale(0.8);
  background:#111;
  padding:30px;
  border:1px solid rgba(218,24,24,0.4);
  text-align:center;
  opacity:0;
  pointer-events:none;
  transition:0.3s;
  z-index:100;
}

.popup::before{
  content:'';
  position:fixed;
  top:0;
  left:0;
  width:100%;
  height:100%;
  background:rgba(0,0,0,0.7);
  z-index:-1;
  opacity:0;
  transition:0.3s;
}

.popup.active::before{
  opacity:1;
}

.popup.active{
  opacity:1;
  transform:translate(-50%, -50%) scale(1);
  pointer-events:auto;
}

.popup h2{
  color:#FFD700;
  font-family:'Bebas Neue',sans-serif;
  letter-spacing:3px;
}

.popup p{
  margin-top:10px;
  color:rgba(255,255,255,0.7);
}

.close{
  margin-top:20px;
  padding:8px 20px;
  background:#DA1818;
  border:none;
  color:white;
  cursor:pointer;
  transition:0.3s;
}

.close:hover{
  background:#ff2a2a;
  box-shadow:0 0 15px rgba(218,24,24,0.6);
}
</style>
</head>

<body>

<div class="bg"></div>

<div class="container">

  <div class="score">49 ALL OUT</div>
  <div class="tagline">STILL UNBEATABLE</div>

  <div class="desc">
    The lowest score in IPL history.<br>
    No collapse has gone lower.
  </div>

  <div class="live">
    AFTER EVERY MATCH → <span>49 REMAINS THE LOWEST</span>
  </div>

  <!-- BUTTON -->
  <button class="btn" onclick="openPopup()">TODAY MATCH LIVE</button>

</div>

<!-- POPUP -->
<div class="popup" id="popup">
  <h2>LIVE MATCH UPDATE</h2>
  <p>Whatever happens today...<br><strong>49 is STILL UNBEATABLE 😎</strong></p>
  <button class="close" onclick="closePopup()">Close</button>
</div>

<script>
function openPopup(){
  document.getElementById("popup").classList.add("active");
}

function closePopup(){
  document.getElementById("popup").classList.remove("active");
}
</script>

</body>
</html>
